
configuration:
  ∧ (4 conjuncts):
    conjunct 1:
      step ?"a" [], branches:
        if False then
            step ?"a" [], branches:
              ⊤ (underspecified)
            step ?"b" [p:Int], branches:
              ⊤ (underspecified)
            step ?"take" [], branches:
              ⊤ (underspecified)
            step ?"water" [p:Int], branches:
              ⊤ (underspecified)
            step !"esp" [p:Int,milk:Bool], branches:
              ⊥ (forbidden)
            step !"tea" [p:Int], branches:
              if (p_1) = (2) then
                  step ?"a" [], branches:
                    ⊤ (underspecified)
                  step ?"b" [p:Int], branches:
                    ⊤ (underspecified)
                  step ?"take" [], branches:
                    ⊤ (underspecified)
                  step ?"water" [p:Int], branches:
                    ⊤ (underspecified)
                  step !"esp" [p:Int,milk:Bool], branches:
                    ⊤ (underspecified)
                  step !"tea" [p:Int], branches:
                    ⊤ (underspecified)
              else:
                  ⊥ (forbidden)
        else:
            ⊤ (underspecified)
      step ?"b" [p:Int], branches:
        ⊤ (underspecified)
      step ?"take" [], branches:
        ⊤ (underspecified)
      step ?"water" [p:Int], branches:
        ⊤ (underspecified)
      step !"esp" [p:Int,milk:Bool], branches:
        ⊥ (forbidden)
      step !"tea" [p:Int], branches:
        ⊥ (forbidden)
    conjunct 2:
      step ?"a" [], branches:
        ⊤ (underspecified)
      step ?"b" [p:Int], branches:
        if ((-p_0)) ≥ 0 then
            step ?"a" [], branches:
              ⊤ (underspecified)
            step ?"b" [p:Int], branches:
              ⊤ (underspecified)
            step ?"take" [], branches:
              ⊤ (underspecified)
            step ?"water" [p:Int], branches:
              ⊤ (underspecified)
            step !"esp" [p:Int,milk:Bool], branches:
              if (p_1) = (p_0) then
                  step ?"a" [], branches:
                    ⊤ (underspecified)
                  step ?"b" [p:Int], branches:
                    ⊤ (underspecified)
                  step ?"take" [], branches:
                    ⊤ (underspecified)
                  step ?"water" [p:Int], branches:
                    ⊤ (underspecified)
                  step !"esp" [p:Int,milk:Bool], branches:
                    ⊤ (underspecified)
                  step !"tea" [p:Int], branches:
                    ⊤ (underspecified)
              else:
                  ⊥ (forbidden)
            step !"tea" [p:Int], branches:
              ⊥ (forbidden)
        else:
            ⊤ (underspecified)
      step ?"take" [], branches:
        ⊤ (underspecified)
      step ?"water" [p:Int], branches:
        ⊤ (underspecified)
      step !"esp" [p:Int,milk:Bool], branches:
        ⊥ (forbidden)
      step !"tea" [p:Int], branches:
        ⊥ (forbidden)
    conjunct 3:
      step ?"a" [], branches:
        ⊤ (underspecified)
      step ?"b" [p:Int], branches:
        if ((-p_0)) ≥ 0 then
            step ?"a" [], branches:
              ⊤ (underspecified)
            step ?"b" [p:Int], branches:
              ⊤ (underspecified)
            step ?"take" [], branches:
              ⊤ (underspecified)
            step ?"water" [p:Int], branches:
              ⊤ (underspecified)
            step !"esp" [p:Int,milk:Bool], branches:
              if milk_1 then
                  step ?"a" [], branches:
                    ⊤ (underspecified)
                  step ?"b" [p:Int], branches:
                    ⊤ (underspecified)
                  step ?"take" [], branches:
                    ⊤ (underspecified)
                  step ?"water" [p:Int], branches:
                    ⊤ (underspecified)
                  step !"esp" [p:Int,milk:Bool], branches:
                    ⊤ (underspecified)
                  step !"tea" [p:Int], branches:
                    ⊤ (underspecified)
              else:
                  ⊥ (forbidden)
            step !"tea" [p:Int], branches:
              ⊥ (forbidden)
        else:
            ⊤ (underspecified)
      step ?"take" [], branches:
        ⊤ (underspecified)
      step ?"water" [p:Int], branches:
        ⊤ (underspecified)
      step !"esp" [p:Int,milk:Bool], branches:
        ⊥ (forbidden)
      step !"tea" [p:Int], branches:
        ⊥ (forbidden)
    conjunct 4:
      step ?"a" [], branches:
        if False then
            step ?"a" [], branches:
              ⊤ (underspecified)
            step ?"b" [p:Int], branches:
              ⊤ (underspecified)
            step ?"take" [], branches:
              ⊤ (underspecified)
            step ?"water" [p:Int], branches:
              ⊤ (underspecified)
            step !"esp" [p:Int,milk:Bool], branches:
              if True then
                  step ?"a" [], branches:
                    ⊤ (underspecified)
                  step ?"b" [p:Int], branches:
                    ⊤ (underspecified)
                  step ?"take" [], branches:
                    ∧ (4 conjuncts):
                      conjunct 1:
                        if True then
                            … (depth limit)
                        else:
                            ⊤ (underspecified)
                      conjunct 2:
                        if True then
                            … (depth limit)
                        else:
                            ⊤ (underspecified)
                      conjunct 3:
                        if True then
                            … (depth limit)
                        else:
                            ⊤ (underspecified)
                      conjunct 4:
                        if True then
                            … (depth limit)
                        else:
                            ⊤ (underspecified)
                  step ?"water" [p:Int], branches:
                    ⊤ (underspecified)
                  step !"esp" [p:Int,milk:Bool], branches:
                    ⊥ (forbidden)
                  step !"tea" [p:Int], branches:
                    ⊥ (forbidden)
              else:
                  ⊥ (forbidden)
            step !"tea" [p:Int], branches:
              if True then
                  step ?"a" [], branches:
                    ⊤ (underspecified)
                  step ?"b" [p:Int], branches:
                    ⊤ (underspecified)
                  step ?"take" [], branches:
                    ∧ (4 conjuncts):
                      conjunct 1:
                        if True then
                            … (depth limit)
                        else:
                            ⊤ (underspecified)
                      conjunct 2:
                        if True then
                            … (depth limit)
                        else:
                            ⊤ (underspecified)
                      conjunct 3:
                        if True then
                            … (depth limit)
                        else:
                            ⊤ (underspecified)
                      conjunct 4:
                        if True then
                            … (depth limit)
                        else:
                            ⊤ (underspecified)
                  step ?"water" [p:Int], branches:
                    ⊤ (underspecified)
                  step !"esp" [p:Int,milk:Bool], branches:
                    ⊥ (forbidden)
                  step !"tea" [p:Int], branches:
                    ⊥ (forbidden)
              else:
                  ⊥ (forbidden)
        else:
            ⊤ (underspecified)
      step ?"b" [p:Int], branches:
        if ((-p_0)) ≥ 0 then
            step ?"a" [], branches:
              ⊤ (underspecified)
            step ?"b" [p:Int], branches:
              ⊤ (underspecified)
            step ?"take" [], branches:
              ⊤ (underspecified)
            step ?"water" [p:Int], branches:
              ⊤ (underspecified)
            step !"esp" [p:Int,milk:Bool], branches:
              if True then
                  step ?"a" [], branches:
                    ⊤ (underspecified)
                  step ?"b" [p:Int], branches:
                    ⊤ (underspecified)
                  step ?"take" [], branches:
                    ∧ (4 conjuncts):
                      conjunct 1:
                        if True then
                            … (depth limit)
                        else:
                            ⊤ (underspecified)
                      conjunct 2:
                        if True then
                            … (depth limit)
                        else:
                            ⊤ (underspecified)
                      conjunct 3:
                        if True then
                            … (depth limit)
                        else:
                            ⊤ (underspecified)
                      conjunct 4:
                        if True then
                            … (depth limit)
                        else:
                            ⊤ (underspecified)
                  step ?"water" [p:Int], branches:
                    ⊤ (underspecified)
                  step !"esp" [p:Int,milk:Bool], branches:
                    ⊥ (forbidden)
                  step !"tea" [p:Int], branches:
                    ⊥ (forbidden)
              else:
                  ⊥ (forbidden)
            step !"tea" [p:Int], branches:
              if True then
                  step ?"a" [], branches:
                    ⊤ (underspecified)
                  step ?"b" [p:Int], branches:
                    ⊤ (underspecified)
                  step ?"take" [], branches:
                    ∧ (4 conjuncts):
                      conjunct 1:
                        if True then
                            … (depth limit)
                        else:
                            ⊤ (underspecified)
                      conjunct 2:
                        if True then
                            … (depth limit)
                        else:
                            ⊤ (underspecified)
                      conjunct 3:
                        if True then
                            … (depth limit)
                        else:
                            ⊤ (underspecified)
                      conjunct 4:
                        if True then
                            … (depth limit)
                        else:
                            ⊤ (underspecified)
                  step ?"water" [p:Int], branches:
                    ⊤ (underspecified)
                  step !"esp" [p:Int,milk:Bool], branches:
                    ⊥ (forbidden)
                  step !"tea" [p:Int], branches:
                    ⊥ (forbidden)
              else:
                  ⊥ (forbidden)
        else:
            ⊤ (underspecified)
      step ?"take" [], branches:
        ⊤ (underspecified)
      step ?"water" [p:Int], branches:
        ∧ (4 conjuncts):
          conjunct 1:
            if True then
                step ?"a" [], branches:
                  if ((p_0+-2)) ≥ 0 then
                      step ?"a" [], branches:
                        ⊤ (underspecified)
                      step ?"b" [p:Int], branches:
                        ⊤ (underspecified)
                      step ?"take" [], branches:
                        ⊤ (underspecified)
                      step ?"water" [p:Int], branches:
                        ⊤ (underspecified)
                      step !"esp" [p:Int,milk:Bool], branches:
                        ⊥ (forbidden)
                      step !"tea" [p:Int], branches:
                        if (p_2) = (2) then
                            … (depth limit)
                        else:
                            ⊥ (forbidden)
                  else:
                      ⊤ (underspecified)
                step ?"b" [p:Int], branches:
                  ⊤ (underspecified)
                step ?"take" [], branches:
                  ⊤ (underspecified)
                step ?"water" [p:Int], branches:
                  ⊤ (underspecified)
                step !"esp" [p:Int,milk:Bool], branches:
                  ⊥ (forbidden)
                step !"tea" [p:Int], branches:
                  ⊥ (forbidden)
            else:
                ⊤ (underspecified)
          conjunct 2:
            if True then
                step ?"a" [], branches:
                  ⊤ (underspecified)
                step ?"b" [p:Int], branches:
                  if ((p_0+-p_1)) ≥ 0 then
                      step ?"a" [], branches:
                        ⊤ (underspecified)
                      step ?"b" [p:Int], branches:
                        ⊤ (underspecified)
                      step ?"take" [], branches:
                        ⊤ (underspecified)
                      step ?"water" [p:Int], branches:
                        ⊤ (underspecified)
                      step !"esp" [p:Int,milk:Bool], branches:
                        if (p_2) = (p_1) then
                            … (depth limit)
                        else:
                            ⊥ (forbidden)
                      step !"tea" [p:Int], branches:
                        ⊥ (forbidden)
                  else:
                      ⊤ (underspecified)
                step ?"take" [], branches:
                  ⊤ (underspecified)
                step ?"water" [p:Int], branches:
                  ⊤ (underspecified)
                step !"esp" [p:Int,milk:Bool], branches:
                  ⊥ (forbidden)
                step !"tea" [p:Int], branches:
                  ⊥ (forbidden)
            else:
                ⊤ (underspecified)
          conjunct 3:
            if True then
                step ?"a" [], branches:
                  ⊤ (underspecified)
                step ?"b" [p:Int], branches:
                  if ((p_0+-p_1)) ≥ 0 then
                      step ?"a" [], branches:
                        ⊤ (underspecified)
                      step ?"b" [p:Int], branches:
                        ⊤ (underspecified)
                      step ?"take" [], branches:
                        ⊤ (underspecified)
                      step ?"water" [p:Int], branches:
                        ⊤ (underspecified)
                      step !"esp" [p:Int,milk:Bool], branches:
                        if milk_2 then
                            … (depth limit)
                        else:
                            ⊥ (forbidden)
                      step !"tea" [p:Int], branches:
                        ⊥ (forbidden)
                  else:
                      ⊤ (underspecified)
                step ?"take" [], branches:
                  ⊤ (underspecified)
                step ?"water" [p:Int], branches:
                  ⊤ (underspecified)
                step !"esp" [p:Int,milk:Bool], branches:
                  ⊥ (forbidden)
                step !"tea" [p:Int], branches:
                  ⊥ (forbidden)
            else:
                ⊤ (underspecified)
          conjunct 4:
            if True then
                step ?"a" [], branches:
                  if ((p_0+-2)) ≥ 0 then
                      step ?"a" [], branches:
                        ⊤ (underspecified)
                      step ?"b" [p:Int], branches:
                        ⊤ (underspecified)
                      step ?"take" [], branches:
                        ⊤ (underspecified)
                      step ?"water" [p:Int], branches:
                        ⊤ (underspecified)
                      step !"esp" [p:Int,milk:Bool], branches:
                        if True then
                            … (depth limit)
                        else:
                            ⊥ (forbidden)
                      step !"tea" [p:Int], branches:
                        if True then
                            … (depth limit)
                        else:
                            ⊥ (forbidden)
                  else:
                      ⊤ (underspecified)
                step ?"b" [p:Int], branches:
                  if ((p_0+-p_1)) ≥ 0 then
                      step ?"a" [], branches:
                        ⊤ (underspecified)
                      step ?"b" [p:Int], branches:
                        ⊤ (underspecified)
                      step ?"take" [], branches:
                        ⊤ (underspecified)
                      step ?"water" [p:Int], branches:
                        ⊤ (underspecified)
                      step !"esp" [p:Int,milk:Bool], branches:
                        if True then
                            … (depth limit)
                        else:
                            ⊥ (forbidden)
                      step !"tea" [p:Int], branches:
                        if True then
                            … (depth limit)
                        else:
                            ⊥ (forbidden)
                  else:
                      ⊤ (underspecified)
                step ?"take" [], branches:
                  ⊤ (underspecified)
                step ?"water" [p:Int], branches:
                  ∧ (4 conjuncts):
                    conjunct 1:
                      if ¬(((p_0+-10)) ≥ 0) then
                          step ?"a" [], branches:
                            if ((p_0+p_1+-2)) ≥ 0 then
                                … (depth limit)
                            else:
                                ⊤ (underspecified)
                          step ?"b" [p:Int], branches:
                            ⊤ (underspecified)
                          step ?"take" [], branches:
                            ⊤ (underspecified)
                          step ?"water" [p:Int], branches:
                            ⊤ (underspecified)
                          step !"esp" [p:Int,milk:Bool], branches:
                            ⊥ (forbidden)
                          step !"tea" [p:Int], branches:
                            ⊥ (forbidden)
                      else:
                          ⊤ (underspecified)
                    conjunct 2:
                      if ¬(((p_0+-10)) ≥ 0) then
                          step ?"a" [], branches:
                            ⊤ (underspecified)
                          step ?"b" [p:Int], branches:
                            if ((p_0+p_1+-p_2)) ≥ 0 then
                                … (depth limit)
                            else:
                                ⊤ (underspecified)
                          step ?"take" [], branches:
                            ⊤ (underspecified)
                          step ?"water" [p:Int], branches:
                            ⊤ (underspecified)
                          step !"esp" [p:Int,milk:Bool], branches:
                            ⊥ (forbidden)
                          step !"tea" [p:Int], branches:
                            ⊥ (forbidden)
                      else:
                          ⊤ (underspecified)
                    conjunct 3:
                      if ¬(((p_0+-10)) ≥ 0) then
                          step ?"a" [], branches:
                            ⊤ (underspecified)
                          step ?"b" [p:Int], branches:
                            if ((p_0+p_1+-p_2)) ≥ 0 then
                                … (depth limit)
                            else:
                                ⊤ (underspecified)
                          step ?"take" [], branches:
                            ⊤ (underspecified)
                          step ?"water" [p:Int], branches:
                            ⊤ (underspecified)
                          step !"esp" [p:Int,milk:Bool], branches:
                            ⊥ (forbidden)
                          step !"tea" [p:Int], branches:
                            ⊥ (forbidden)
                      else:
                          ⊤ (underspecified)
                    conjunct 4:
                      if ¬(((p_0+-10)) ≥ 0) then
                          step ?"a" [], branches:
                            if ((p_0+p_1+-2)) ≥ 0 then
                                … (depth limit)
                            else:
                                ⊤ (underspecified)
                          step ?"b" [p:Int], branches:
                            if ((p_0+p_1+-p_2)) ≥ 0 then
                                … (depth limit)
                            else:
                                ⊤ (underspecified)
                          step ?"take" [], branches:
                            ⊤ (underspecified)
                          step ?"water" [p:Int], branches:
                            ∧ (4 conjuncts):
                              conjunct 1:
                                if ¬(((p_0+p_1+-10)) ≥ 0) then
                                    … (depth limit)
                                else:
                                    ⊤ (underspecified)
                              conjunct 2:
                                if ¬(((p_0+p_1+-10)) ≥ 0) then
                                    … (depth limit)
                                else:
                                    ⊤ (underspecified)
                              conjunct 3:
                                if ¬(((p_0+p_1+-10)) ≥ 0) then
                                    … (depth limit)
                                else:
                                    ⊤ (underspecified)
                              conjunct 4:
                                if ¬(((p_0+p_1+-10)) ≥ 0) then
                                    … (depth limit)
                                else:
                                    ⊤ (underspecified)
                          step !"esp" [p:Int,milk:Bool], branches:
                            ⊥ (forbidden)
                          step !"tea" [p:Int], branches:
                            ⊥ (forbidden)
                      else:
                          ⊤ (underspecified)
                step !"esp" [p:Int,milk:Bool], branches:
                  ⊥ (forbidden)
                step !"tea" [p:Int], branches:
                  ⊥ (forbidden)
            else:
                ⊤ (underspecified)
      step !"esp" [p:Int,milk:Bool], branches:
        ⊥ (forbidden)
      step !"tea" [p:Int], branches:
        ⊥ (forbidden)
