
configuration:
  ∧ (4 conjuncts):
    step [subst: {x_0:=0}], branches:
      ⊤ (underspecified)
    step [subst: {x_0:=0}], branches:
      ⊤ (underspecified)
    step [subst: {x_0:=0}], branches:
      ⊤ (underspecified)
    step [subst: {x_0:=0}], branches:
      ∧ (4 conjuncts):
        if True then
            step [subst: {x_1:=p_0}], branches:
              ⊤ (underspecified)
        else: ⊤
        if True then
            step [subst: {x_1:=p_0}], branches:
              if True then
                  leaf: True
              else: ⊤
        else: ⊤
        if True then
            step [subst: {x_1:=p_0}], branches:
              if True then
                  leaf: True
              else: ⊤
        else: ⊤
        if True then
            step [subst: {x_1:=p_0}], branches:
              if True then
                  leaf: True
              else: ⊤
        else: ⊤
