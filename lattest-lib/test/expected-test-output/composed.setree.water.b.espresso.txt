
configuration:
  ∧ (4 conjuncts):
    step [subst: {x_0:=0}], branches:
      ⊤ (underspecified)
    step [subst: {x_0:=0}], branches:
      ⊤ (underspecified)
    step [subst: {x_0:=0}], branches:
      ⊤ (underspecified)
    step [subst: {x_0:=0}], branches:
      ∧ (4 conjuncts):
        if True then
            step [subst: {x_1:=p_0}], branches:
              ⊤ (underspecified)
        else: ⊤
        if True then
            step [subst: {x_1:=p_0}], branches:
              if True then
                  step [subst: {x_2:=p_1}], branches:
                    if (p_2) = (p_1) then
                        leaf: True
                    else: ⊥
              else: ⊤
        else: ⊤
        if True then
            step [subst: {x_1:=p_0}], branches:
              if True then
                  step [subst: {x_2:=p_0}], branches:
                    if milk_2 then
                        leaf: True
                    else: ⊥
              else: ⊤
        else: ⊤
        if True then
            step [subst: {x_1:=p_0}], branches:
              if True then
                  step [subst: {x_2:=p_0}], branches:
                    if True then
                        leaf: True
                    else: ⊥
              else: ⊤
        else: ⊤
